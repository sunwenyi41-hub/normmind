# NormMind 开发进度追踪

本文档用于管理「规范智能知识问答 Agent」的阶段开发进度。项目采用 `spec-kit` SDD 工作方式推进：每个阶段对应一个独立 spec 单元，开发完成后必须先更新本文档，再进入下一阶段。

## 使用规则

- 每个阶段开始前，先在对应阶段下补充 spec 名称、负责人、开始日期。
- 每个阶段完成后，必须更新本文档中的状态、完成说明、遗留事项和验收结果。
- 若阶段范围发生变化，先更新本文件，再调整对应的 spec、plan、tasks。
- 未在本文件中标记为完成的阶段，不视为已交付。

## 状态说明

- `[ ]` 未开始
- `[~]` 进行中
- `[x]` 已完成
- `[!]` 阻塞中

---

## 阶段 1：基础加固

建议 spec 单元：`foundation-hardening`

目标：把当前已有的登录、问答、Supabase 持久化、Coze 调用链路做稳，形成可持续开发的基础闭环。

状态：`[x] 已完成`

TODO LIST

- [x] 检查并修复历史会话仅显示一条的问题
- [x] 统一会话创建、续问、删除、标题生成逻辑
- [x] 统一登录态恢复、未登录拦截和跳转体验
- [x] 统一 Coze 成功、超时、无引用、异常四类响应处理
- [x] 补充开发用测试数据和最小 seed 数据
- [x] 验证 Supabase RLS、用户数据隔离和 API 所有权校验
- [x] 完成阶段验收并更新本文档

验收标准

- 一个账号可以稳定查看多条历史对话
- 新建对话、继续追问、删除对话均可正常使用
- Coze 失败时页面不崩，且给出明确反馈
- 测试数据可重复生成并支持开发联调

完成说明

- 已完成真实反馈工单读取与状态流转代码，并修复 `messages_json` 快照模式下的反馈关联。
- 已补 QA 样本、评测记录表、RLS 策略、测试数据及后台真实数据页面。
- 已完成会话创建 / 续问 / 删除 / 标题更新的一体化改造。
- 已将问答链路切换为 AI SDK `UIMessage[]` 持久化模型，并兼容旧消息结构回读。
- 已统一 Coze 的完成、证据不足、失败与兜底 fallback 行为。
- 已补登录后的安全跳转与 OAuth / 邮箱确认失败提示。
- 已补开发环境一键生成测试会话能力，便于联调与演示。

遗留事项

- 已在真实 Supabase 项目上完成普通用户 / 管理员跨账号 RLS 回归。

---

## 阶段 2：问答工作台

建议 spec 单元：`qa-workbench`

目标：把当前聊天页升级为专业工作台，突出检索模式、引用追溯、结果处理和多轮追问体验。

状态：`[x] 已完成`

TODO LIST

- [x] 改造首页进入路径与问答工作台主入口
- [x] 完成左侧会话流、筛选区和快速新建对话
- [x] 完成中间问答区，支持普通模式与深度模式切换
- [x] 完成右侧信息栏，展示引用、处理状态和结果操作
- [x] 补充复制、重新生成、反馈、收藏等操作入口
- [x] 补充空状态引导、推荐问题和响应式布局优化
- [x] 完成阶段验收并更新本文档

验收标准

- 主界面具备专业工作台结构，而非单纯聊天页
- 用户可在同一界面完成提问、查看引用、继续追问和结果处理
- 桌面端与移动端主流程可用

完成说明

- 已切换到 `useChat + DefaultChatTransport + LangChain createAgent().stream()` 主链路。
- 会话主存储已迁移为 Supabase `conversations.messages_json` 快照，历史接口支持从旧 `messages` 行式结构回读兼容。
- Coze 已改为优先走单轮 Bot 调用，旧 Workflow 仅作为兜底 fallback。
- 问答区已升级为工作台结构：左侧会话流，中间问答区，右侧引用 / 处理链路面板。

遗留事项

- 仍需补验证真实 Coze Bot v3 返回结构与当前解析器是否完全一致。
- 仍需补最小 seed 数据、RLS 越权测试与登录态体验回归。

---

## 阶段 3：引用追溯与 PDF 原文查看

建议 spec 单元：`citation-and-pdf-viewer`

目标：让引用卡片真正能追溯到原文文档，至少支持页码级定位，提升答案可信度。

状态：`[~] 进行中`

TODO LIST

- [x] 统一引用数据结构和前端卡片渲染
- [x] 建立规范文档与本地或存储 PDF 的关联关系
- [x] 实现“查看原文”入口和 PDF 打开流程
- [x] 支持基于页码的原文定位
- [x] 对无法定位页码或段落的情况提供兜底提示
- [ ] 验证移动端和桌面端的 PDF 查看体验
- [ ] 完成阶段验收并更新本文档

验收标准

- 用户点击引用后可以打开关联 PDF
- 至少可定位到具体页码
- 无法精确定位段落时有明确说明，不伪装成精确定位

完成说明

- 已支持引用卡片与本地 PDF 资源映射。
- 已支持基于引用页码或 URL hash 的 PDF 页码定位。
- 已在右侧信息栏中补充原文片段 / PDF 预览双视图。
- 已对“只能定位页码、不能精确到段落”的情况做显式提示，不伪装成精准定位。
- 已补充 PDF 定位提示、可复制的人工核对信息以及页内检索词，降低“打开 PDF 但不知道去哪找”的使用阻力。
- 已优化右侧引用面板在窄屏设备下的布局，移动端会以全宽抽屉展示，PDF 预览高度也已做响应式调整。

遗留事项

- 仍需在真实移动端浏览器中回归 PDF 预览的可用性；当前 Codex 内置浏览器对 localhost 存在连接策略限制，未完成可视化回归截图。
- 仍缺少段落级锚点，高亮能力需要知识库或 Coze 返回更细粒度定位信息。

---

## 阶段 4：规范资料库

建议 spec 单元：`knowledge-library`

目标：为问答工作台补上资料库能力，形成“问答 + 文档”的产品结构。

状态：`[~] 进行中`

TODO LIST

- [x] 建立资料库列表页与文档详情页
- [x] 支持规范名称、版本、专业分类、标签、状态展示
- [x] 提供上传流程原型：上传、解析状态、标签确认、发布
- [x] 补充检索范围选择：全库、指定规范、企业库占位
- [x] 预留与 Coze / 外部知识库同步的扩展接口
- [x] 准备一批测试文档元数据用于联调演示
- [ ] 完成阶段验收并更新本文档

验收标准

- 用户可浏览和检索资料库，而不仅是直接提问
- 文档具备版本、标签、状态等基础信息
- 上传与发布流程可演示、可验证

完成说明

- 已将规范资料库从占位卡片升级为可筛选、可浏览、可查看详情的工作台模块。
- 已补充本地测试文档元数据，覆盖名称、编号、分类、状态、标签、更新时间、知识片段数与摘要。
- 已在详情区补充 PDF 原文入口，并为后续“设为推荐范围”与知识库同步预留交互位置。
- 已补完整的前端入库流程原型：上传文件、模拟解析进度、确认标签与摘要、选择发布范围，并在发布后将资料插入列表顶部供演示联调。
- 已补独立 `/library` 路由页，支持在正式模式与预览模式下直接访问资料库，不再局限于工作台内嵌视图。
- 已补独立 `/library/[id]` 资料详情页，支持从资料库详情面板继续进入路由级资料页。
- 已补 `/settings` 账户设置骨架页，承接用户端“账户设置”信息架构并展示登录方式状态与问答偏好。

遗留事项

- 上传、解析、标签确认、发布目前仍为前端流程原型，尚未接入真实存储、后台任务与外部知识库写入。
- 后续仍需将独立资料详情页与真实数据库 / 存储记录打通。

---

## 阶段 5：管理后台与质量运营

建议 spec 单元：`admin-and-quality-ops`

目标：建立最小管理后台，支撑 QA 样本、评测、反馈分析和 Agent 调优。

状态：`[x] 已完成`

TODO LIST

- [x] 建立管理员入口与最小权限边界
- [x] 建立 QA 样本管理页
- [x] 建立评测集运行与结果查看页
- [x] 建立用户反馈与问题工单列表
- [x] 提供 Agent 配置说明页或配置入口占位
- [x] 提供 RAG / 引用质量审计页占位
- [x] 完成阶段验收并更新本文档

验收标准

- 管理端与用户端边界清晰
- QA 样本、反馈、评测数据可查看和维护
- 后续 Agent 调优具备基础操作入口

完成说明

- 已补齐 `admin-and-quality-ops` Speckit SDD 单元，后续后台开发以该单元的 `tasks.md` 追踪。
- 已增加服务端管理员邮箱 / 受信任角色校验，普通用户无法看到入口或直接访问后台路由。
- 已补独立 `/admin` 管理后台路由，支持正式模式与预览模式访问。
- 已搭建最小后台骨架，覆盖 QA 样本管理、评测结果查看、用户反馈与问题工单列表。
- 已补 Agent 配置说明区块，用于承接普通模式 / 深度模式工作流与后续 RAG 审计信息。
- 已将管理后台入口接入工作台、资料库、资料详情页和账户设置页，形成用户端 / 管理端基础导航。
- 已将后台拆分为多页面结构：`/admin/qa`、`/admin/evals`、`/admin/feedback`、`/admin/agent`，便于后续逐页接入真实数据。
- 已补 `/admin/audit` 质量审计页，占位展示召回、重排、引用完整率与页码定位质量等指标。
- 已在真实 Supabase 项目应用后台迁移，并以普通 / 管理员 JWT 完成会话、QA 和评测越权回归。

遗留事项

- 管理员权限边界、真实数据与越权回归已完成。
- QA 样本已支持新增，后续版本再补评测自动执行与跨模块联动。

---

## 阶段 6：部署上线与发布收口

建议 spec 单元：`release-and-deploy`

目标：完成 GitHub、Vercel、Supabase 和环境变量层面的上线准备，形成可回归、可发布、可维护的首版。

状态：`[~] 进行中`

TODO LIST

- [x] 清理并补齐 README、环境变量示例和部署说明
- [x] 校验本地、Preview、Production 环境变量配置
- [x] 配置并验证 Vercel Preview / Production 部署
- [x] 修复邮箱确认回调，兼容 PKCE `code` 与 `token_hash` 两种邮件确认格式
- [x] 增加忘记密码邮件、恢复会话与设置新密码完整流程（已兼容回调目标丢失）
- [x] 优化过期密码恢复链接的错误落地页
- [x] 修复线上回答流暴露内部工具片段导致部分浏览器页面崩溃的问题
- [x] 将邮箱登录、注册、重置密码改为站点服务端代理调用 Supabase，避免外部用户浏览器直连 Supabase 失败
- [x] 优化 Supabase Auth 域名不可达时的页面降级，避免旧登录态浏览器进入全局错误页
- [x] 增加会话列表与消息快照容错清洗，避免普通账号历史数据结构异常导致工作台渲染失败
- [x] 准备测试账号、测试数据和上线检查清单
- [ ] 跑通登录、问答、历史、引用、反馈全链路回归
- [ ] 复查 Supabase RLS、密钥边界和错误监控
- [ ] 完成阶段验收并更新本文档

验收标准

- `main` 分支可稳定部署
- Preview 环境可联调，Production 配置清晰
- 上线前检查项齐全，核心链路已回归验证

完成说明

- 已补充 `README.md`，明确预览入口、环境变量说明、登录能力状态与部署顺序建议。
- 已补充 `docs/release-checklist.md`，覆盖环境变量矩阵、测试账号、核心链路回归、RLS 校验、安全边界与回滚建议。
- 已同步更新 `.env.example` 与 `AGENTS.md`，使阶段 6 的部署文档与协作要求可追踪。
- 已升级 Vercel CLI 至 54.21.1，并将现有必需环境变量同步到 Development / Preview / Production。
- 已完成 Vercel Production 首次部署：`https://normmind.vercel.app`，状态 Ready，线上首页健康检查通过。
- 已部署邮箱确认回调修复；生产环境根路径收到 PKCE `code` 时返回 307 至 `/auth/callback`，并设置 `private, no-store`。
- 已部署忘记密码与设置新密码闭环；无有效恢复会话访问 `/reset-password` 会安全返回登录页。
- 已兼容 Supabase 恢复邮件回落根路径且丢失 `next` 的情况：近期恢复请求会自动进入 `/reset-password`。
- 已将根路径收到的 `otp_expired` / `access_denied` 转为登录页友好提示，避免停留在原始错误 URL。
- 已过滤 LangChain / Coze 内部工具调用流片段，浏览器端只接收阶段状态、答案文本与引用元数据，避免 `toolCallId` / `inputTextDelta` 类内部事件造成页面崩溃。
- 已补充全局中文错误兜底页，即使前端渲染异常也会提供重试与返回工作台入口。
- 已新增 `/api/auth/email` 站点内登录代理，邮箱登录 / 注册 / 重置密码由 Vercel 服务端连接 Supabase，降低部分网络环境下浏览器直连 Supabase 导致 `Failed to fetch` 的风险。
- 已为首页、登录页、后台布局和会话刷新中间件补充 Supabase Auth 网络异常捕获；当 Supabase 项目域名不可解析时，页面会安全回到登录页，登录接口会返回中文认证服务不可达提示。
- 已为 `messages_json` 与会话摘要增加服务端 / 客户端双侧结构清洗；旧版、缺字段或异常结构的普通账号历史记录不会再直接触发工作台全页崩溃。

遗留事项

- 若普通账号仍出现全局错误页，需要补采该账号浏览器控制台错误或临时清理该账号异常会话快照。
- 当前生产日志显示 `oftbihnmtsvugonhxoli.supabase.co` 在 Vercel 运行时出现 `getaddrinfo ENOTFOUND`，需要在 Supabase Dashboard 核对项目是否暂停、Project URL 是否变化，并同步更新 Vercel 环境变量。
- Production 已上线；尚未建立 GitHub PR 自动 Preview 流程。
- 邮箱确认已兼容 Supabase PKCE `code` 回落到站点根路径及 `token_hash` 邮件模板两种流程。
- 手机号注册按当前产品决定暂不开放。
- GitHub remote 尚未建立；Supabase / GitHub 连接器令牌过期，需重新登录后完成迁移、回调域名与推送。
- 尚未完成真实登录、问答、历史、引用、反馈链路的上线前回归记录。
- 尚未补充 Production 级管理员权限与后台真实数据联动的上线说明。

---

## 总体决策建议

- 建议内测目标设在阶段 4 完成后进行，这时产品会更像完整的专业工具而非 Demo。
- 若需要尽快试用，可在阶段 3 完成后启动受控内测，但需明确资料库与后台能力尚未完成。
- 默认开发顺序：阶段 1 → 阶段 2 → 阶段 3 → 阶段 4 → 阶段 5 → 阶段 6。

## 进度更新要求

每次完成一个阶段后，必须同步更新以下内容：

- 当前阶段状态：改为 `[x] 已完成`
- 完成说明：记录本阶段交付结果
- 遗留事项：记录进入下一阶段前暂缓的问题
- 若有范围变更：补充调整原因

未更新 `progress.md` 的阶段，视为未完成，不进入下一阶段开发。
